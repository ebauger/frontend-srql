// place files you want to import through the `$lib` alias in this folder.
import { setSurrealProvider, getSurrealProvider } from '$lib/SurrealProvider.svelte';

export { setSurrealProvider, getSurrealProvider };
